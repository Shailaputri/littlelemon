from django.tests import TestCase

pass
# from restaurant import views, models
# from setuptools import setup
# from django.urls import reverse

# class MenuViewTest(TestCase):
# 	setup(
# 		falafel = models.Menu.objects.create(title="Falafel", price=12, inventory=5),
# 		burger = models.Menu.objects.create(title="Burger", price=5, inventory=25),
# 		burrito = models.Menu.objects.create(title="Burrito", price=50, inventory=5),

# 		)

# 	def test_menu_view():
# 		url = reverse("views.MenuView")
# 		resp = get(url)
# 		assertEqual(resp.status_code, 200)
# 		self.assertIN(falafel.title, resp.content)



